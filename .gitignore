# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local test presets (third-party MilkDrop/Butterchurn packs, not app code)
sample-presets

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Internal planning/process artifacts (specs, plans, SDD workspaces) — not
# part of the shipped app, kept local rather than pushed to GitHub.
docs
.superpowers

# Spotify Client ID — not a secret, but keeps per-developer config out of git.
.env
